; Set max file size to 100MB
upload_max_filesize = 100M

; Set max POST size to 100MB (must be >= upload_max_filesize)
post_max_size = 100M

; Set memory limit (must be >= post_max_size)
; Your current limit is 128M, but we'll raise it just in case.
memory_limit = 256M
